services:
  - type: web
    name: incident-agent
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: AIPIPE_TOKEN
        sync: false
      - key: MODEL_NAME
        value: openai/gpt-4o-mini
